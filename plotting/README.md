# Plotting

This directory contains figure-oriented scripts separated from the main analysis modules.

- `main_figures/`: representative scripts and notebooks used for core manuscript figures.
- `extended_data/`: supporting figure generation and comparison notebooks.

Where figure scripts depend on processed intermediate objects, keep those intermediates outside the repository and document the expected inputs near the top of each plotting notebook or in future figure-specific README files.
